const xss = require('xss')

const GigsService = {
  getAllGigs(db) {},

  getById(db, id) {},

  deleteById(db, id) {},

  createGig(db, newGig) {},

  updateGig(db, id, newGig) {},

  serializeGig(gig) {},
  serializeGigs(gigs) {}
}

module.exports = GigsService